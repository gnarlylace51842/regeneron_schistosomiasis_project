# Runs Folder

Use `runs/` for training-related outputs such as:

- checkpoints
- logs
- learning-curve exports
- saved predictions produced during training

Suggested convention:

- `runs/smoke/` for very small verification runs
- `runs/experiments/` for real experiments

